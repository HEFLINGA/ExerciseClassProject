@model ExerciseProgram.Domain.Entities.Exercise

<div class="well">
    <h3>
        <strong>@Model.exerciseName</strong>
        <span class="pull-right label label-primary">@Model.exerciseMax.ToString()</span>
    </h3>
    <span class="lead"> @Model.exerciseDesc</span>
</div>